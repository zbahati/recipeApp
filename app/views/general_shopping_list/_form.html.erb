<%= form_with(model: general_shopping_list) do |form| %>
  <% if general_shopping_list.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(general_shopping_list.errors.count, "error") %> prohibited this general_shopping_list from being saved:</h2>

      <ul>
        <% general_shopping_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :index, style: "display: block" %>
    <%= form.text_field :index %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
